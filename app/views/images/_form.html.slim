= simple_form_for @image do |f|
  = f.input :link, :as => :string, :input_html => { :class => 'oversize input-text' }
  = f.input :comment, :input_html => { :class => 'niceTextarea' }
  = f.submit :class => 'blue nice button radius'