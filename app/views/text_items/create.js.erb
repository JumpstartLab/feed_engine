<%= render :partial => "support/create_item", :locals => { :stream_item  => @new_stream_item, :errors => @text_item.errors, :type => "text" } %>

<% if @text_item.errors.empty? %>
  $('#text_item_body').val('');
<%end %>
