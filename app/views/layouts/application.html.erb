<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "FeedEngine" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <script type="text/javascript" src="http://use.typekit.com/uby6cjl.js"></script>
    <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
    <!-- <link href="images/favicon.ico" rel="shortcut icon"> -->
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>


    <div class="container">
        <ul class="nav nav-section">
          <% if user_signed_in? %>
            <li class="offset8 pull-left navigation-item"><%= link_to image_tag("../images/home.png", :title => "Home"), root_path%></li>
            <li class="pull-left navigation-item"><%= link_to image_tag("../images/dashboard.png", :title => "Dashboard"), dashboard_path %></li>
            <li class="pull-left navigation-item"><%= link_to image_tag("../images/logout.png", :title => "Logout"), destroy_user_session_path, :method => :delete %></li>
          <% else %>
            <a href="<%= new_user_path %>">
              <li class="offset6 pull-left, navigation-item-signup">
                <span class="raphael-icon">]</span>Sign Up
              </li>
            </a>
          <% end %>
        </ul>
      <% flash.each do |name, msg| %>
        <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
          <a class="close" data-dismiss="alert">×</a>
          <%= msg %>
        </div>
      <% end %>

      <div class="content">
        <div class="row">
          <div class="offset1 span9">
            <%= yield %>
          </div>
        </div><!--/row-->
      </div><!--/content-->

      <footer>
      <p>&copy; Company 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
