<% if current_user %>
  <% @items.each do |item| %>
    <div class="row feed_row">
      <%= render :partial => "item", :locals => {:item => item} %>
    </div>
  </div>
<% end %>

<%= paginate @stream_items %>
<% end %>
