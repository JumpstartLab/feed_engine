<%= render :partial => "support/create_item", :locals => { :stream_item  => @new_stream_item, :errors => @image_item.errors, :type => "image" } %>

<% if @image_item.errors.empty? %>
  $('#image_item_url').val('');
  $('#image_item_comment').val('');
<%end %>

