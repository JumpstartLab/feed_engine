require 'spec_helper'

describe User do
  context "who is not authenticated" do
    pending "does not see refeed links"
  end

  context "who is authenticated" do
    pending "sees refeed links"
    pending "clicks refeed and sees the refed item in their feed"
  end
end
